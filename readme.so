# 🛰️ Projeto WowKI – Monitoramento de Velocidade de Jogadoras via IoT  
### Sprint 4 – Edge Computing | Arquitetura IoT

---

## 👥 Integrantes

| Nome | RM |
|:-------------------------------|:-------:|
| Ben Hur Iung de Lima Ferreira | 561564 |
| Erick Munhoes Ancias | 561484 |
| João Paulo Fernandes | 563430 |
| João Pedro Mendes de Figueiredo | 558779 |
| Patrick Canuto | 563776 |

---

## 🧠 Descrição do Projeto

O projeto **WowKI – IoT EdgeCPT** tem como objetivo demonstrar a integração entre dispositivos IoT e uma aplicação web em tempo real, voltada ao **monitoramento de desempenho de jogadoras de futebol**.  
A solução identifica **qual jogadora está com a maior velocidade**, atualizando as informações dinamicamente a cada segundo.

A arquitetura foi desenvolvida para funcionar em três camadas principais:

- **Camada IoT (ESP32 + Simulação Wokwi)**  
  Cada dispositivo IoT representa uma jogadora e envia, via protocolo HTTP, sua velocidade atual para o servidor.
  
- **Camada Backend (Flask)**  
  Atua como o ponto central de comunicação, recebendo as leituras dos sensores e armazenando os valores mais recentes.
  
- **Camada Frontend (HTML + JavaScript)**  
  Exibe as velocidades individuais de cada jogadora e destaca automaticamente a atleta com o melhor desempenho no momento.

---

## ⚙️ Estrutura de Pastas

📂 WowKI_IoT_EdgeCPT
├── 📁 iot
│ └── wowki_simulador.ino
├── 📁 backend
│ └── app.py
└── 📁 frontend
├── index.html
└── script.js

yaml
Copiar código

---

## 💻 Tecnologias Utilizadas

- C / C++ (Arduino IDE / Wokwi)  
- ESP32 (Wi-Fi integrado)  
- Python (Flask + Flask-CORS)  
- HTML5 / CSS3 / JavaScript puro  
- Protocolo HTTP / JSON  
- Simulação via Wokwi  

---

## 🔩 Arquitetura de Comunicação (Pub/Sub)

A comunicação segue o padrão **Publish/Subscribe**:

- **Publisher:** o código do ESP32 (`wowki_simulador.ino`) publica as velocidades periodicamente via `HTTP POST`.  
- **Broker (Servidor Flask):** o `app.py` recebe os dados, armazena em memória e fornece acesso aos últimos valores coletados.  
- **Subscriber (Frontend):** o `script.js` realiza requisições `HTTP GET` a cada 2 segundos, exibindo em tela o status atualizado das jogadoras.

---

## 📶 IoT (wowki_simulador.ino)

- Simula diferentes jogadoras, cada uma com seu nome único.  
- Gera valores aleatórios de velocidade (1–10 m/s).  
- Envia os dados em formato JSON para o servidor Flask.  
- Permite a execução de várias instâncias simultâneas no **Wokwi**, simulando um ambiente multiusuário.

Exemplo de payload enviado:
```json
{
  "jogadora": "Jogadora_A",
  "velocidade": 7.5
}
🖥️ Backend (app.py)
Desenvolvido com Flask e CORS habilitado.

Rota /api/velocidade:

POST: recebe os dados do IoT e atualiza a memória local.

GET: retorna a lista de jogadoras com suas velocidades atuais e identifica automaticamente a mais rápida.

Exemplo de resposta JSON:

json
Copiar código
{
  "todas": {
    "Jogadora_A": 7.5,
    "Jogadora_B": 6.9
  },
  "mais_rapida": "Jogadora_A",
  "velocidade": 7.5
}
🌐 Frontend (index.html + script.js)
Interface minimalista, sem frameworks.

Exibe todas as jogadoras e suas velocidades em tempo real.

Destaca o nome da jogadora com maior velocidade.

Atualiza automaticamente a cada 2 segundos.

🧪 Resultados da PoC (Prova de Conceito)
Durante a simulação, foi possível demonstrar o fluxo completo de dados:

O ESP32 (no Wokwi) enviou valores de velocidade ao servidor Flask.

O backend processou e armazenou as informações em tempo real.

O dashboard web refletiu automaticamente as mudanças, mostrando qual jogadora estava mais rápida.

Essa integração comprovou o funcionamento completo da arquitetura IoT proposta, de ponta a ponta.

🚀 Como Executar o Projeto
1️⃣ Pré-requisitos
Python 3 instalado

Arduino IDE ou Wokwi (para simulação)

Conexão com o servidor IP público
54.172.140.81

2️⃣ Acessar o Servidor
bash
Copiar código
ssh nomedeusuario@54.172.140.81
3️⃣ Executar o Backend
bash
Copiar código
cd WowKI_IoT_EdgeCPT/backend
python3 app.py
Servidor ativo em:

cpp
Copiar código
http://54.172.140.81:5000
4️⃣ Rodar o IoT (ESP32 no Wokwi)
Abra iot/wowki_simulador.ino.

Edite a linha do servidor:

cpp
Copiar código
String serverUrl = "http://54.172.140.81:5000/api/velocidade";
Inicie a simulação.

Crie múltiplas abas (uma por jogadora, mudando o nome no código).

5️⃣ Abrir o Frontend
Abra frontend/index.html no navegador.

O painel mostrará as velocidades atualizadas em tempo real.

🧭 Garantia de Replicabilidade
O projeto foi organizado em pastas modulares para fácil replicação.
Qualquer grupo pode executar os mesmos passos em um servidor Linux ou VM com Python e Wi-Fi habilitado.

🏁 Conclusão
O WowKI IoT EdgeCPT evidencia a eficiência de uma arquitetura IoT simples, leve e funcional.
Através de uma simulação prática, foi possível integrar hardware, backend e interface web de forma síncrona, demonstrando um fluxo contínuo de dados e análises em tempo real.

Esse modelo pode ser facilmente expandido para projetos de monitoramento esportivo, saúde, ou performance física de atletas.
